#include <stdio.h>
int main (){

    // Augmented assignment operators are used to replace a statement where an operator takes a variable as one of its own arguments and then assign results back to the same varaible
    int x = 10;

    //x = x + 2;
    //x+=2;

    //x = x-3;
    //x-=3;

    //x = x * 4;
    //x*=4;

    //x = x / 5;
    //x/=2;

    //x = x % 2;
    x%=2;







    printf("%d",x);

    return 0;
}